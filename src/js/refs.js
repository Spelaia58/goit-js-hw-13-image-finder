const hitsContainer = document.querySelector('.gallery');
const searchForm = document.querySelector('#search-form');
const loadMoreBtn = document.querySelector('[data-action="load-more"]');
const loadMoreBtnLable = document.querySelector('.label');
const loadMoreBtnSpinner = document.querySelector('.spinner');
const openModalImg = document.querySelector('.js-gallery');

export default {
  hitsContainer,
  searchForm,
  loadMoreBtn,
  loadMoreBtnLable,
  loadMoreBtnSpinner,
  openModalImg,
};
